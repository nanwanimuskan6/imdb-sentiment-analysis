# Planned tests

Add tests alongside implementation for schema and label validation, cleaning,
duplicate-aware split isolation, training-only vectorizer fitting, metric correctness,
artifact loading, and inference input validation. Add small integration smoke tests
for training and prediction; normal tests should not require full IMDb downloads
or full DistilBERT fine-tuning. No tests are implemented in this scaffold.
