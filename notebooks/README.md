# Exploratory notebooks

Future notebooks will document dataset exploration and visual interpretation.
Reusable logic belongs in `src/imdb_sentiment/`; notebooks will call that code.
Keep committed notebooks free of large embedded datasets and outputs.
